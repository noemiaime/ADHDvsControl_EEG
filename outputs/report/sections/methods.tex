\section{Metodi}

\subsection{Dataset}

Il dataset utilizzato è disponibile su Kaggl (\cite{Kaggle}). Esso contiene registrazioni EEG di 61 bambini con ADHD e 60 soggetti di controllo, di età compresa tra 7 e 12 anni, registrati durante un compito di attenzione visiva in cui si mostra ai bambini immagini di personaggi di cartoni animati. La distribuzione delle classi risulta approssimativamente bilanciata.

I dati sono organizzati in formato tabellare: ogni riga corrisponde a un campione temporale del segnale EEG, mentre le colonne contengono i valori dei canali EEG, l'identificativo del soggetto e la classe diagnostica. Le registrazioni sono state acquisite con frequenza di campionamento$
f_s = 128 \ \text{Hz}$.

Il dataset contiene 19 canali EEG:
\\
\texttt{Fp1}, \texttt{Fp2}, \texttt{F3}, \texttt{F4}, \texttt{C3}, \texttt{C4}, \texttt{P3}, \texttt{P4}, \texttt{O1}, \texttt{O2}, \texttt{F7}, \texttt{F8}, \texttt{T7}, \texttt{T8}, \texttt{P7}, \texttt{P8}, \texttt{Fz}, \texttt{Cz}, \texttt{Pz}.

I canali seguono la nomenclatura del sistema internazionale 10-20. È uno standard utilizzato per descrivere la posizione degli elettrodi sullo scalpo in modo riproducibile tra soggetti diversi. In questo sistema la distanza tra gli elettrodi che viene calcolata in percentuali (10$\%$ o 20$\%$) rispetto alla lunghezza della testa, misurata in senso fronto-occipitale e laterale. Le lettere dei canali indicano approssimativamente la regione corticale sottostante: \texttt{Fp} indica la regione frontopolare, \texttt{F} la regione frontale, \texttt{C} la regione centrale, \texttt{P} la regione parietale, \texttt{O} la regione occipitale e \texttt{T} la regione temporale. I numeri dispari indicano elettrodi localizzati prevalentemente sull'emisfero sinistro, mentre i numeri pari indicano elettrodi localizzati prevalentemente sull'emisfero destro. Gli elettrodi indicati con la lettera \texttt{z}, come \texttt{Fz}, \texttt{Cz} e \texttt{Pz}, si trovano lungo la linea mediana.

 Questo raggruppamento consente di analizzare le feature sia a livello di singolo canale sia a livello regionale, confrontando informazioni più localizzate con rappresentazioni più sintetiche dell'attività EEG.

Durante la registrazione non sono presenti variazioni di condizione associate al target e al task. Per questo motivo, le serie temporali EEG sono state trasformate in feature soggetto-livello. In questo modo, ogni soggetto è rappresentato da una singola riga nella tabella finale delle feature.

\subsection{Feature extraction}

L'estrazione delle feature è stata effettuata a partire dalle serie temporali EEG di ciascun soggetto. Per ogni soggetto e per ogni canale EEG, il segnale temporale è stato trasformato nel dominio della frequenza mediante stima della Power Spectral Density.

La Power Spectral Density, o PSD, descrive come la potenza del segnale è distribuita sulle diverse frequenze.
La PSD è stata stimata utilizzando il metodo di Welch (\cite{Welch}). Tale metodo suddivide il segnale in finestre temporali parzialmente sovrapposte, calcola lo spettro per ciascuna finestra e infine media gli spettri ottenuti.

I parametri utilizzati per il metodo di Welch sono stati:

$
n_{\text{perseg}} = 512,
\qquad
n_{\text{overlap}} = 256
$

Poiché la frequenza di campionamento è pari a 128 Hz, una finestra di 512 campioni corrisponde a $\frac{512}{128} = 4 \ \text{s}$

La risoluzione in frequenza della PSD è quindi $ \Delta f =
\frac{f_s}{n_{\text{perseg}}}= \frac{128}{512} = 0.25 \ \text{Hz}$.

A partire dalla PSD sono state calcolate le potenze nelle principali bande EEG: delta, theta, alpha e beta. Gli intervalli di frequenza utilizzati sono riportati nella Tabella~\ref{tab:eeg_bands}.

\begin{table}[H]
	\centering
	\begin{tabular}{lc}
		\hline
		\textbf{Banda EEG} & \textbf{Intervallo di frequenza} \\
		\hline
		Delta $(\delta)$ & 1--4 Hz \\
		Theta $(\theta)$ & 4--8 Hz \\
		Alpha $(\alpha)$ & 8--12 Hz \\
		Beta $(\beta)$ & 12--30 Hz \\
		\hline
	\end{tabular}
	\caption{Bande di frequenza utilizzate per il calcolo delle feature spettrali.}
	\label{tab:eeg_bands}
\end{table}

La potenza di ciascuna banda è stata calcolata integrando la PSD nell'intervallo di frequenze corrispondente ($f_1, f_2$):

\begin{equation}
	P_{\text{band}} =\int_{f_1}^{f_2} PSD(f),df
\end{equation} 

In aggiunta alle potenze di banda, è stato calcolato il rapporto tra theta e beta, definito come:

\begin{equation}
	TBR =\frac{P_{\theta}}{P_{\beta}}
\end{equation}

Ogni feature è stato calcolata sia a livello di singolo canale sia a livello regionale mediando le feature dei canali appartenenti alla stessa regione. 

L'output finale della feature extraction è una tabella soggetto-livello contenente potenze di banda per ciascun canale, rapporti theta/beta per ciascun canale, potenze medie regionali, rapporti theta/beta regionali e metadati del soggetto, come identificativo, classe e durata della registrazione.


\subsection{Esplorazione delle feature}

Dopo l'estrazione delle feature, è stata eseguita un'analisi esplorativa per studiare la distribuzione delle variabili e confrontare i due gruppi ADHD e Control.

In primo luogo, sono state calcolate statistiche descrittive per ciascuna feature, tra cui media, deviazione standard, minimo, massimo e quartili. Questa fase ha permesso di osservare la scala delle variabili, la loro variabilità e la possibile presenza di valori estremi.

\subsubsection{Test di Mann-Whitney U}

Per confrontare ciascuna feature tra soggetti ADHD e soggetti Control è stato utilizzato il test di Mann-Whitney U.

Questo test è un test non parametrico per il confronto tra due gruppi indipendenti (\cite{test}). È stato scelto perché le feature EEG possono non seguire una distribuzione normale e possono contenere outlier. A differenza del test t di Student, il test di Mann-Whitney non confronta direttamente le medie dei due gruppi, ma confronta i ranghi dei valori.

Il procedimento consiste nel considerare insieme tutti i valori della feature nei due gruppi, ordinarli dal più piccolo al più grande e assegnare a ciascun valore un rango. Se i due gruppi provengono dalla stessa distribuzione, i ranghi dovrebbero essere distribuiti in modo simile tra ADHD e Control. Se invece un gruppo tende ad avere valori più alti o più bassi, anche i ranghi tenderanno a essere sistematicamente più alti o più bassi.

Per un gruppo con $n_1$ osservazioni, la statistica $U_1$ può essere scritta come:
\begin{equation}
	U_1 =R_1 -\frac{n_1(n_1+1)}{2}
\end{equation}) 

dove $R_1$ è la somma dei ranghi del primo gruppo. 

La statistica U finale del test viene calcolata come il valore minimo tra $U_1$ e la controparte $U_2$ del secondo gruppo. 

Nel presente progetto è stato utilizzato un test a due code, poiché non si è imposto a priori che le feature dovessero essere maggiori nel gruppo ADHD o nel gruppo Control. In questo modo, il test è sensibile a differenze in entrambe le direzioni.

Il p-value ottenuto dal test indica la probabilità di osservare una differenza nei ranghi almeno pari a quella osservata, assumendo vera l'ipotesi nulla. L'ipotesi nulla è che la feature abbia distribuzioni identiche nei due gruppi.

\subsubsection{Cohen's d}

Oltre alla significatività statistica, è stata calcolata una misura della grandezza dell'effetto tramite Cohen's $d$.

Il p-value indica se una differenza è statisticamente significativa, ma non descrive direttamente quanto tale differenza sia grande. Per questo motivo, Cohen's $d$ è stato utilizzato per quantificare la differenza standardizzata tra le medie dei due gruppi.

Cohen's $d$ è definito come:

\begin{equation}
	d =\frac{\mu_{ADHD} - \mu_{Control}}{s_{pooled}}
\end{equation}

dove $\mu_{ADHD}$ è la media della feature nel gruppo ADHD, $\mu_{Control}$ è la media della feature nel gruppo Control e $s_{pooled}$ è la deviazione standard pooled.

La deviazione standard pooled si definisce come:

\begin{equation}
	s_{pooled}=\sqrt{\frac{s_{ADHD}^{2} + s_{Control}^{2}}{2}}
\end{equation}

dove $s_{ADHD}$ e $s_{Control}$ sono le deviazioni standard nei due gruppi.

Un valore positivo di Cohen's $d$ indica che la feature ha valori medi più alti nel gruppo ADHD, mentre un valore negativo indica valori medi più alti nel gruppo Control. Il valore assoluto di Cohen's $d$ è stato utilizzato per ordinare le feature in base alla grandezza della differenza tra i gruppi.

In questo modo è stato possibile identificare le feature con la maggiore separazione tra ADHD e Control, indipendentemente dalla direzione della differenza.

\subsubsection{Correzione per confronti multipli}

Poiché il confronto statistico è stato ripetuto su molte feature EEG, è stato necessario correggere i p-value per confronti multipli (\cite{multiple}).

Eseguendo diversi test statistici aumenta la probabilità di ottenere risultati apparentemente significativi solo per caso. Per ridurre questo rischio è stata utilizzata la procedura Benjamini-Hochberg per il controllo del False Discovery Rate.

Il False Discovery Rate controlla la proporzione attesa di falsi positivi tra le feature dichiarate significative.

La procedura Benjamini-Hochberg ordina i p-value dal più piccolo al più grande:

\begin{equation}
	p_{(1)} \leq p_{(2)} \leq ... \leq p_{(m)}
\end{equation}

dove $m$ è il numero totale di test eseguiti.

Ogni p-value ordinato viene confrontato con una soglia che dipende dalla sua posizione $ \frac{i}{m}Q$ dove $i$ è il rango del p-value ordinato, $m$ è il numero totale di test e $Q$ è il livello di False Discovery Rate scelto.

Nel progetto sono state considerate significative dopo correzione le feature con:

$
p_{FDR} < 0.05
$

\subsubsection{Analisi di correlazione}

Dopo il confronto tra gruppi, è stata analizzata la correlazione tra tutte le feature EEG numeriche.

Molte feature EEG possono essere correlate tra loro perché derivano dalla stessa banda di frequenza, da canali spazialmente vicini o da rapporti calcolati a partire dalle stesse grandezze.

Per questa analisi è stata utilizzata la correlazione di Spearman, una misura non parametrica di associazione monotona tra due variabili. A differenza della correlazione di Pearson, Spearman non richiede che la relazione tra le variabili sia lineare e non richiede l'assunzione di normalità.

Il coefficiente di Spearman si basa sui ranghi delle osservazioni. Date due variabili $X$ e $Y$, i valori vengono trasformati nei rispettivi ranghi $R(X)$ e $R(Y)$, e la correlazione viene calcolata sui ranghi.

Nel caso senza ranghi uguali, il coefficiente di Spearman può essere scritto come:

\begin{equation}
	\rho =1 -\frac{6 \sum_{i=1}^{n} d_i^2}{n(n^2 - 1)}
\end{equation}

dove $d_i$ è la differenza tra i ranghi delle due variabili per l'osservazione $i$, e $n$ è il numero di osservazioni.

Il coefficiente $\rho$ varia tra $-1$ e $1$ e $\rho = 1$ indica una relazione monotona positiva perfetta e $ \rho = -1$ indica una relazione monotona negativa perfetta, mentre $ \rho = 0$ indica assenza di relazione monotona.


\subsection{Classificazione con Random Forest}

Per valutare se le feature EEG estratte permettono di distinguere soggetti ADHD e Control, è stato utilizzato un classificatore Random Forest.

Prima dell'addestramento, il dataset soggetto-livello è stato diviso in due sottoinsiemi:

\begin{itemize}
	\item [--] training/validation set, pari all'80% dei soggetti;
	\item [--] test set indipendente, pari al 20% dei soggetti.
\end{itemize}

La divisione è stata effettuata in modo da mantenere proporzioni simili di soggetti ADHD e Control nei due sottoinsiemi.

Il test set è stato mantenuto separato durante la fase di selezione del modello ed è stato utilizzato solo per la valutazione finale. Questa scelta riduce il rischio di data leakage.

Sul training/validation set è stata eseguita una cross-validation stratificata a 5 fold. In ogni iterazione, il modello è stato addestrato su quattro fold e validato sul fold rimanente. La procedura è stata ripetuta cinque volte, in modo che ogni fold fosse utilizzato una volta come validation set.

Sono stati confrontati tre insiemi di feature:

\begin{enumerate}
	\item tutte le feature EEG;
	\item solo le feature a livello di singolo canale;
	\item solo le feature medie regionali.
\end{enumerate}

Questo confronto permette di valutare se la classificazione dipenda maggiormente da informazioni locali a livello di elettrodo, oppure da pattern più globali a livello regionale.

Il classificatore Random Forest presenta diversi iperparametri, vedi Sezione 2.5.1. È stato inoltre utilizzato il parametro $\texttt{class\_weight="balanced"}$ per compensare eventuali differenze nella distribuzione delle classi.

Le metriche utilizzate per valutare le prestazioni del modello sono state:

\begin{itemize}
	\item [--] accuracy;
	\item [--] balanced accuracy;
	\item [--] precision;
	\item [--] recall;
	\item [--] F1-score;
	\item [--] ROC-AUC;
	\item [--] confusion matrix.
\end{itemize}

La balanced accuracy è stata utilizzata come metrica principale per confrontare i feature set.

Dopo il confronto in cross-validation, il miglior feature set è stato selezionato in base alla balanced accuracy media. ì

\subsubsection{Tuning degli iperparametri}

Il tuning degli iperparametri è stato eseguito sul miglior feature set selezionato nella fase precedente, utilizzando esclusivamente il training/validation set. 

L'ottimizzazione degli iperparametri è stata effettuata in due passaggi. È stata inzialmente utilizzata una \texttt{RandomizedSearchCV} per esplorare in modo efficiente uno spazio ampio di possibili combinazioni di iperparametri. Successivamente, è stata applicata una \texttt{GridSearchCV} locale attorno alla migliore combinazione individuata dalla ricerca randomizzata, con l'obiettivo di raffinare ulteriormente la selezione dei parametri.

Entrambe le fasi di tuning sono state eseguite mediante cross-validation stratificata a 5 fold sul training/validation set. La metrica utilizzata per selezionare la migliore combinazione di iperparametri è stata la balanced accuracy, poiché tiene conto delle performance su entrambe le classi.

Lo spazio di ricerca iniziale utilizzato nella \texttt{RandomizedSearchCV} è riportato nella Tabella~\ref{tab:rf_randomized_hyperparameter_space}.

\begin{table}[H]
	\centering
	\small
	\begin{tabular}{ll}
		\hline
		\textbf{Iperparametro} & \textbf{Valori o intervallo testato} \\
		\hline
		\texttt{n\_estimators} & interi tra 200 e 1500 \\
		\texttt{max\_depth} & \texttt{None}, 3, 5, 7, 10, 15, 20, 30 \\
		\texttt{min\_samples\_leaf} & interi tra 1 e 9 \\
		\texttt{min\_samples\_split} & interi tra 2 e 14 \\
		\texttt{max\_features} & \texttt{sqrt}, \texttt{log2}, \texttt{None} \\
		\hline
	\end{tabular}
	\caption{Spazio di ricerca degli iperparametri utilizzato nella \texttt{RandomizedSearchCV}.}
	\label{tab:rf_randomized_hyperparameter_space}
\end{table}

Dopo la ricerca randomizzata, è stata costruita una griglia locale centrata sui migliori valori individuati. Per i parametri numerici sono stati testati valori vicini al valore ottimale trovato dalla \texttt{RandomizedSearchCV}; per \texttt{max\_depth} e \texttt{max\_features} sono stati mantenuti anche valori alternativi comuni. 

Dopo la selezione finale degli iperparametri, il modello Random Forest è stato riaddestrato sull'intero training/validation set e valutato una sola volta sul test set.

\subsubsection{Feature importance}

Dopo l'addestramento del modello finale, sono stati estratti i valori di feature importance della Random Forest.

La feature importance della Random Forest si basa sulla riduzione media dell'impurità, chiamata anche mean decrease in impurity. In un albero decisionale, ogni nodo viene diviso scegliendo la feature e la soglia che permettono di separare meglio le classi.

Come misura comune dell'impurità si usa la Gini impurity:

\begin{equation}
	Gini = 1 - p_{ADHD}^{2} - p_{Control}^{2}
\end{equation}

dove $p_k$ è la proporzione di campioni appartenenti alla classe $k$ nel nodo.

La riduzione di impurità prodotta da uno split è calcolata come:

\begin{equation}
	\Delta I = I_{parent}\left(\frac{N_{left}}{N_{parent}} I_{left}+ \frac{N_{right}}{N_{parent}} I_{right}\right)
\end{equation}

Una feature viene considerata importante se viene utilizzata in split che riducono fortemente l'impurità. Nella Random Forest, l'importanza viene calcolata mediando il contributo della feature su tutti gli alberi e normalizzando i valori.
